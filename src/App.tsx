import ErrorPage from '@/components/auth/ErrorPage/ErrorPage'

export function App() {
  return (
    <div
      style={{
        alignItems: 'center',
        display: 'flex',
        justifyContent: 'center',
      }}
    >
      <ErrorPage />
    </div>
  )
}
